<div class="container-fluid bg-dark bg-opacity-75 text-light">
	<div class="row py-5 justify-content-center">
		<div class="col-12 col-lg-3 p-3 bg-dark justify-content-center">
			<div class="text-center"><a href="{{user_url}}" class="fs-3 sptGrn">{{user}}</a></div>
			<img src="{{user_image}}" class="rounded-circle img-fluid p-3">
		</div>
		<!-- I'm removing this functionality due to time constraints
		<div class="col-12 col-lg-3 text-light">
			<p class="text-start">Number of playlists: {{playlists_length}} (max 50)</p>
			<ul class="text-start ps-0">Overall Average Scores
				<li>
					[Ac] Acousticness: [placeholder]
				</li>
				<li>
					[Da] Danceability: [placeholder]
				</li>
				<li>
					[En] Energy: [placeholder]
				</li>
				<li>
					[In] Instrumentalness: [placeholder]
				</li>
				<li>
					[Li] Liveness: [placeholder]
				</li>
				<li>
					[Lo] Loudness: [placeholder]
				</li>
				<li>
					[Sp] Speechiness: [placeholder]
				</li>
				<li>
					[Va] Valence: [placeholder]
				</li>
			</ul>
		</div> -->
	</div>
	<div class="row bg-dark bg-opacity-75 py-3 justify-content-center">
			{{#each playlists}}
			<div class="col-12 col-lg-3 m-3 p-3 bg-dark justify-content-center">
				<div class="text-center"><a href="{{link}}" class="fs-3 sptGrn">{{name}}</a></div>
				<img src="{{image}}" class="rounded-circle img-fluid p-3">
			</div>
			{{else}}
            <div class="col-12 col-lg-3 m-3 p-3 bg-dark justify-content-center">
				<p class="empty">Uh oh! You don't appear to have any playlists. :(</p>
            </div>
			{{/each}}
	</div>
</div>
